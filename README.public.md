# OAuth2 Sidecar for Kubernetes

This is the public-facing README. The primary content has been moved to `README.new.md` and will replace the root README when ready.

See:
- `README.new.md` for full documentation
- `helm/oauth2-sidecar/README.md` for Helm-specific docs
- `examples/` for example applications
