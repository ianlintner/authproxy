---
# OAuth2 Proxy Secret - EXAMPLE
# Copy this file to secret.yaml and fill in your OAuth credentials
#
# To generate cookie-secret (use a cryptographically secure method):
#   openssl rand -base64 32 | tr -- '+/' '-_'
#   OR
#   python -c 'import os,base64; print(base64.urlsafe_b64encode(os.urandom(32)).decode())'
#
# IMPORTANT: Never commit secret.yaml with real credentials to version control!
#
apiVersion: v1
kind: Secret
metadata:
  name: oauth2-proxy-secret
  namespace: default
  labels:
    app.kubernetes.io/name: oauth2-proxy-sidecar
    app.kubernetes.io/component: authentication
type: Opaque
stringData:
  # OAuth Client ID from your OAuth provider (GitHub, Google, etc.)
  # Replace with your actual client ID
  client-id: "<YOUR_CLIENT_ID>"
  
  # OAuth Client Secret from your OAuth provider
  # Replace with your actual client secret
  client-secret: "<YOUR_CLIENT_SECRET>"
  
  # Cookie encryption secret (must be 32 bytes, base64 encoded)
  # Generate using: openssl rand -base64 32 | tr -- '+/' '-_'
  cookie-secret: "<GENERATED_COOKIE_SECRET>"
